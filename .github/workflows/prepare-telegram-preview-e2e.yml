name: Prepare Telegram Preview E2E

on:
  push:
    branches:
      - agent/telegram-order-invite-flow
    paths:
      - .github/workflows/prepare-telegram-preview-e2e.yml

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: team_cAthcmyw4079BDgelX0YjG9i
      VERCEL_PROJECT_ID: prj_9LIdNafm4JYpYCNA6ujbIlQs2Bh9
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/telegram-order-invite-flow
          fetch-depth: 0

      - name: Validate current branch
        run: |
          node --check api/telegram-setup.js
          node --check api/telegram-connect.js
          node --check api/telegram-webhook.js
          node --check utils/telegram.js
          git diff --check origin/main...HEAD

      - name: Install Vercel CLI and link project
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npm install --global vercel@latest
          mkdir -p .vercel
          printf '{"orgId":"%s","projectId":"%s"}\n' "$VERCEL_ORG_ID" "$VERCEL_PROJECT_ID" > .vercel/project.json
          vercel project inspect --token="$VERCEL_TOKEN" >/dev/null

      - name: Save existing Preview Admin password
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          vercel env pull /tmp/preview-env --yes --environment=preview --token="$VERCEL_TOKEN"
          set -a
          source /tmp/preview-env
          set +a
          if [ -z "${ADMIN_PASSWORD:-}" ]; then
            echo "Existing Preview ADMIN_PASSWORD is unexpectedly missing"
            exit 1
          fi
          echo "::add-mask::$ADMIN_PASSWORD"
          echo "OLD_ADMIN_PASSWORD=$ADMIN_PASSWORD" >> "$GITHUB_ENV"
          rm -f /tmp/preview-env

      - name: Temporarily isolate Preview Admin password
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          ADMIN_PASSWORD=$(openssl rand -hex 32)
          echo "::add-mask::$ADMIN_PASSWORD"
          printf '%s' "$ADMIN_PASSWORD" | vercel env update ADMIN_PASSWORD preview --yes --token="$VERCEL_TOKEN"
          echo "ADMIN_PASSWORD=$ADMIN_PASSWORD" >> "$GITHUB_ENV"
          echo "Preview-only ADMIN_PASSWORD temporarily isolated"

      - name: Redeploy Preview only
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          PREVIEW_URL=$(vercel deploy --yes --token="$VERCEL_TOKEN")
          echo "PREVIEW_URL=$PREVIEW_URL" >> "$GITHUB_ENV"
          echo "Preview redeployed: $PREVIEW_URL"

      - name: Register Telegram webhook on protected Preview
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          vercel curl "$PREVIEW_URL/api/telegram-setup" \
            -X POST \
            -H "X-Admin-Password: $ADMIN_PASSWORD" \
            -H "Content-Type: application/json" \
            -d '{}' > /tmp/setup.json
          node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('/tmp/setup.json','utf8'));if(!j.success){console.error(j);process.exit(1)};if(!j.previewProtectionBypass){console.error('Preview webhook did not enable protection bypass');process.exit(1)};console.log('Webhook registered on protected Preview');"

      - name: Create prefixed Telegram test course
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          cat > /tmp/course.json <<'JSON'
          {
            "slug": "__clone_factory_test_telegram_oneclick_20260811",
            "courseName": "__clone_factory_test Telegram One Click 20260811",
            "price": "0",
            "active": true,
            "sort_order": 9999,
            "description": "Temporary Clone E2E Telegram course",
            "deliveryMode": "telegram",
            "telegramInviteTtlHours": 72
          }
          JSON
          vercel curl "$PREVIEW_URL/api/courses" \
            -X POST \
            -H "X-Admin-Password: $ADMIN_PASSWORD" \
            -H "Content-Type: application/json" \
            --data-binary @/tmp/course.json > /tmp/course-response.json
          COURSE_ID=$(node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('/tmp/course-response.json','utf8'));if(!j.success||!j.data?.id){console.error(j);process.exit(1)};process.stdout.write(j.data.id)")
          echo "COURSE_ID=$COURSE_ID" >> "$GITHUB_ENV"
          echo "Created test course: $COURSE_ID"

      - name: Create one-click Telegram connect URL
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          printf '{"courseId":"%s"}\n' "$COURSE_ID" > /tmp/connect-request.json
          vercel curl "$PREVIEW_URL/api/telegram-connect" \
            -X POST \
            -H "X-Admin-Password: $ADMIN_PASSWORD" \
            -H "Content-Type: application/json" \
            --data-binary @/tmp/connect-request.json > /tmp/connect-response.json
          CONNECT_URL=$(node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('/tmp/connect-response.json','utf8'));if(!j.success||!j.connectUrl){console.error(j);process.exit(1)};process.stdout.write(j.connectUrl)")
          echo "CONNECT_URL=$CONNECT_URL" >> "$GITHUB_ENV"
          echo "### Telegram E2E ready" >> "$GITHUB_STEP_SUMMARY"
          echo "Preview: $PREVIEW_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "Course ID: $COURSE_ID" >> "$GITHUB_STEP_SUMMARY"
          echo "Connect URL: $CONNECT_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "Connect URL: $CONNECT_URL"

      - name: Restore shared Preview Admin password
        if: always() && env.OLD_ADMIN_PASSWORD != ''
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          printf '%s' "$OLD_ADMIN_PASSWORD" | vercel env update ADMIN_PASSWORD preview --yes --token="$VERCEL_TOKEN"
          echo "Shared Preview ADMIN_PASSWORD restored for future deployments"
