name: Deploy Student Manager Fix

on:
  push:
    branches: [main]
    paths: [.github/workflows/deploy-student-manager-fix.yml]

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: team_cAthcmyw4079BDgelX0YjG9i
      VERCEL_PROJECT_ID: prj_9LIdNafm4JYpYCNA6ujbIlQs2Bh9
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Validate checkout API fix
        run: |
          node --check api/register.js
          grep -q "managerPath = '/my-courses.html?registered=1&course='" api/register.js
          grep -q "orderId, deliveryMode, managerPath" api/register.js
          ! grep -q "checkoutDeliveryMode" api/register.js
          git diff --check
      - name: Deploy Commerce Production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          test -n "$VERCEL_TOKEN"
          npm install --global vercel@latest >/dev/null 2>&1
          mkdir -p .vercel
          printf '{"orgId":"%s","projectId":"%s"}\n' "$VERCEL_ORG_ID" "$VERCEL_PROJECT_ID" > .vercel/project.json
          vercel pull --yes --environment=production --token="$VERCEL_TOKEN" >/dev/null
          vercel deploy --prod --yes --token="$VERCEL_TOKEN"
      - name: Remove one-time workflow
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/deploy-student-manager-fix.yml
          git commit -m "chore: remove one-time Commerce deploy workflow"
          git push origin HEAD:main
